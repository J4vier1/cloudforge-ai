# Terraform

This folder will hold infrastructure modules for Azure and Huawei Cloud landing zones.

Initial roadmap:

- `modules/networking`: VNet/VPC, subnets, VPN-ready routing.
- `modules/compute`: target VM sizing and migration landing resources.
- `environments/dev`: first deployable environment.
- `environments/prod`: production-ready environment after the API stabilizes.
