apiVersion: v1
kind: Service
metadata:
  name: cloud-migration-assistant
  namespace: cloudforge-ai
spec:
  type: ClusterIP
  selector:
    app: cloud-migration-assistant
  ports:
    - name: http
      port: 80
      targetPort: 8000
